import { Helper } from "../../types/helper.ts";
import { FlowStep, FlowStepElementTemplate } from "../../types/flow.ts";

export type VisualElement = FlowStep | Helper;
export type VisualElementTemplate = FlowStepElementTemplate;
